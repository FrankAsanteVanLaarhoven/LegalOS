/**
 * Accepted: prohibited phrases in prose only. This file mentions
 * RepositoryContext, workspace_members, SESSION_COOKIE and ACTIVE_ORG_COOKIE
 * in comments and must not be flagged.
 */
export const note = "see the ADR for organisationId handling";
