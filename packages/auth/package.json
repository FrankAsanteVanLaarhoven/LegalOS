{
  "name": "@legalos/auth",
  "version": "0.2.0-dev",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "types": "./src/index.ts",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@legalos/identity": "workspace:*"
  }
}
